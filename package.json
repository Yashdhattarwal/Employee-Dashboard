{
  "name": "employee-dashboard",
  "version": "1.0.0",
  "description": "Full-stack Employee Dashboard",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npm rebuild --prefix backend sqlite3 --build-from-source"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
